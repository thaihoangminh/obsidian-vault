
- A cross-platform JavaScript runtime environment.
- Runs the [[V8 JavaScript engine]] (the core of Google Chrome) outside the browser
[[Blocking vs Non-blocking Paradigms]]: It runs in a single thread - one thread for every request and provides a set of asynchronous I/O primitives that prevent JavaScript code from blocking